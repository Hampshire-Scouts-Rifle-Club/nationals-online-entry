export type CampHelper = {
  firstName: string;
  lastName: string;
  scoutAssociationId: string;
};

export const EmptyCampHelper: CampHelper = {
  firstName: '',
  lastName: '',
  scoutAssociationId: '',
};
